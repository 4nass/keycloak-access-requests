package ch.anass.keycloak.accessrequests.core.domain;

/**
 * Identifies how a notification recipient is resolved by an infrastructure adapter.
 */
public enum AccessRequestNotificationRecipientType {
    USER,
    REALM_ROLE
}
